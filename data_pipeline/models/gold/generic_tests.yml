version: 2

models:
  - name: fct_orders
    columns:
      - name: order_date
        tests:
          - not_null:
              config:
                severity: error

      - name: items_cnt
        tests:
          - not_null:
              config:
                severity: error

      - name: gross_item_sales_amount
        tests:
          - not_null:
              config:
                severity: error

      - name: item_discount_amount
        tests:
          - not_null:
              config:
                severity: error
